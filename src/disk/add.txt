add:
    push    %rbp
    mov     %rsp,%rbp
    mov     %rdi,-0x18(%rbp)
    mov     %rsi,-0x20(%rbp)
    mov     -0x18(%rbp),%rdx
    mov     -0x20(%rbp),%rax
    add     %rdx,%rax
    mov     -0x8(%rbp),%rax
    pop     %rbp
    retq

main:
    mov     %rdx,%rsi
    mov     %rax,%rdi
    callq   5fa <add>
    mov     %rax,-0x8(%rbp)